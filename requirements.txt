# auditoria-eg2026 — dependencias Python
# Última revisión: 2026-04-21
# Activar venv: py -m venv .venv && .\.venv\Scripts\Activate.ps1
# Instalar:     pip install -r requirements.txt

# ── Core datos (Pandas PROHIBIDO para >100k filas) ──
polars==1.6.0
duckdb==1.2.1
pyarrow==18.0.0
numpy==2.1.3
scipy==1.14.1

# ── HTTP ──
requests==2.32.3
aiohttp==3.11.18

# ── Visualización ──
matplotlib==3.9.2

# ── Informes ──
python-docx==1.1.2
Pillow==11.0.0

# ── IPFS / HuggingFace ──
boto3==1.35.74
huggingface-hub==0.26.2

# ── Telegram ──
python-telegram-bot==21.9

# ── Entorno ──
python-dotenv==1.0.1

# ── Agentes IA (FORENSIS) ──
anthropic>=0.96.0

# ── Testing ──
pytest==8.3.4
pytest-cov==6.0.0
